# Configures which features we want to support from the Vireo engine
# Anything that defines feature exclusion or alternative configurations
# should be put in this file. For instance, eventually we'll be replacing JS
# calls with hooks into low level Pico features.